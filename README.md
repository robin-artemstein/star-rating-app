# React star rating application

This is the private repository for React star rating application, the front end tech stack combination is React +Vite + TypeScript + Tailwind + React icons.

## Getting Started

First, install all dependencies.
```
bun install
```
Then run the dev server.
```
bun run dev
```

Open [http://localhost:5173](http://localhost:5173) with your browser to see the result.

You can start editing the page by modifying `src/App.tsx`. The page auto-updates as you edit the file.

## Push codes of the project onto GitHub repository
```
git init
git add .
git commit -m "The Nth commit on date."
git remote rm origin
git branch -M main
git remote add origin git@github.com:robin-artemstein/star-rating-app.git
git push -u -f origin main
```